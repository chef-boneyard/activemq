# Built by Chef. All changes will be overriden
<%
  {
    'activemq.username' => 'system',
    'activemq.password' => 'manager',
    'guest.password' => 'password',
  }.merge(node[:activemq][:configuration]).each do |key, value|
%>
<%= key %>=<%= value %>
<% end %>
