# Defines users that can access the web (console, demo, etc.)
# username: password [,rolename ...]
<%= node['activemq']['admin_console']['credentials']['user_name'] %>: <%= node['activemq']['admin_console']['credentials']['password'] %>, admin
